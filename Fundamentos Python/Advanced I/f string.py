# sem f-string:
current = 1
previous = 0
result = current + previous
print("Current Number " + str(current) + " Previous Number " + str(previous) + " Sum: " + str(result))

# com f-string:
current = 1
previous = 0
result = current + previous
print(f"Current Number {current} Previous Number {previous} Sum: {result}")
